{
  "name": "onlychords",
  "version": "0.0.1",
  "description": "Only the Chords",
  "keywords": [
    "Music",
    "Chords",
    "Guitar",
    "Fretboard"
  ],
  "license": "",
  "author": "Tyler Vick <tyler@tjvick.com>",
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead"
  ],
  "scripts": {
    "build": "parcel build index.html && npm run copy-fonts",
    "dev": "parcel index.html --open",
    "copy-fonts": "cp -PR static/fonts ./dist",
    "format": "prettier --write .",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.8.3",
    "@parcel/transformer-webmanifest": "^2.8.3",
    "parcel": "^2.8.3",
    "prettier": "2.8.8"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.3.0"
  }
}
